#favorites
  #index
    %h1
      Favoriten
    %p
      Übersicht über deine Favoriten:

    -@favorites.each do |favorite|
      .favContainer
        =render :partial => "favorites/favheart", :locals => {:video => favorite.video}
        =render favorite.video